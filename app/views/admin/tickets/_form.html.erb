<%= form_for [:admin, ticket] do |f| %>
  <%= render 'common/errors', resource: @ticket %>

  <p>
    <%= f.label :passenger_id, "User:" %>
    <%= f.collection_select :passenger_id, Passenger.all, :id, :email %>
  </p>

  <p>
    <%= f.label :train_id, "Train:" %>
    <%= f.collection_select :train_id, Train.all, :id, :number %>
  </p>

  <p>
    <%= f.label :first_station_id, "Begin Station:" %>
    <%= f.collection_select :first_station_id, RailwayStation.all, :id, :title %>
  </p>

  <p>
    <%= f.label :last_station_id, "End Station:" %>
    <%= f.collection_select :last_station_id, RailwayStation.all, :id, :title %>
  </p>

  <p>
    <%= f.label :passenger_fio, "Passenger FIO:" %>
    <%= f.text_field :passenger_fio %>
  </p>

  <p>
    <%= f.label :passport_number, "Passport:" %>
    <%= f.text_field :passport_number %>
  </p>
  
  <%= f.submit "#{ticket.new_record? ? 'Создать' : 'Изменить'}" %>
<% end %>
