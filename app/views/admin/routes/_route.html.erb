<tr>
  <td><%= route.name %></td>
  <td><%= link_to t('common.show'), [:admin, route], :class => "btn btn-primary" %></td>
  <td><%= link_to t('common.edit'), edit_admin_route_path(route), :class => "btn btn-primary" %></td>
  <td><%= link_to t('common.destroy'), [:admin, route], method: :delete, data: { confirm: 'Вы уверены?' }, :class => "btn btn-primary" %></td>
</tr>
